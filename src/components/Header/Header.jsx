import { Logo } from "../Logo/Logo";
import { SocialIcons } from "../SocialIcons/SocialIcons";

export const Header = () => {
  return (
    <header>
      <Logo />
      <SocialIcons />
    </header>
  );
};
