import styled from "styled-components";
import { color, space, flexbox, position, layout } from "styled-system";

const Box = styled.div`
  ${color}
  ${space}
  ${flexbox}
  ${position}
  ${layout}
`;

export default Box;
