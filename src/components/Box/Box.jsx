import styled from "styled-components";
import {
  color,
  space,
  flexbox,
  position,
  layout,
  typography,
} from "styled-system";

export const Box = styled.div`
  ${color}
  ${space}
  ${flexbox}
  ${position}
  ${layout}
  ${typography}
`;
